// Get user Viewport usefull for future function uses
var userViewWidth = window.innerWidth;
var userViewHeight = window.innerHeight;
let location = window.location;
// REUSE CODE for a back button based on user history most likeley my home page
function goback(){
  window.history.back();
}
